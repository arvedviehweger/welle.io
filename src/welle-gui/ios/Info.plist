<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>en</string>
    <key>CFBundleDisplayName</key>
    <string>welle.io</string>
    <key>CFBundleExecutable</key>
    <string>$(EXECUTABLE_NAME)</string>
    <key>CFBundleIdentifier</key>
    <string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>$(PRODUCT_NAME)</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>2.7</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSRequiresIPhoneOS</key>
    <true/>
    <key>MinimumOSVersion</key>
    <string>$(IPHONEOS_DEPLOYMENT_TARGET)</string>
    <key>UILaunchScreen</key>
    <dict/>
    <key>UISupportedInterfaceOrientations</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>
    <key>UISupportedInterfaceOrientations~ipad</key>
    <array>
        <string>UIInterfaceOrientationPortrait</string>
        <string>UIInterfaceOrientationPortraitUpsideDown</string>
        <string>UIInterfaceOrientationLandscapeLeft</string>
        <string>UIInterfaceOrientationLandscapeRight</string>
    </array>

    <!-- Required so iOS lets the app reach an rtl_tcp server on the local network -->
    <key>NSLocalNetworkUsageDescription</key>
    <string>welle.io connects to an rtl_tcp server on your local network to receive DAB/DAB+ radio.</string>

    <!-- welle.io does not use the camera or microphone, but the Qt Multimedia
         framework requires these keys or the app aborts on startup. -->
    <key>NSMicrophoneUsageDescription</key>
    <string>welle.io does not record audio. This entry is required by the Qt Multimedia framework.</string>
    <key>NSCameraUsageDescription</key>
    <string>welle.io does not use the camera. This entry is required by the Qt Multimedia framework.</string>

    <!-- Keep audio playing when the app is in the background -->
    <key>UIBackgroundModes</key>
    <array>
        <string>audio</string>
    </array>

    <!-- Avoids the export-compliance prompt on TestFlight; the app uses no custom encryption -->
    <key>ITSAppUsesNonExemptEncryption</key>
    <false/>
</dict>
</plist>
